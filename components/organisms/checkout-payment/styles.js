import styled from "styled-components";

export const Styles = styled.div`
  .checkout-payment__header {
    color: var(--gray-15);
    text-transform: uppercase;
    font-weight: bold;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--gray-4);
    margin-bottom: 1.5rem;
  }

  .checkout-payment__payment-cards {
    margin-bottom: 1.5rem;
  }
`;
