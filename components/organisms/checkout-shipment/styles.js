import styled from "styled-components";

export const Styles = styled.div`
  .checkout-shipment__header {
    color: var(--gray-15);
    text-transform: uppercase;
    font-weight: bold;

    margin-bottom: 1rem;
  }
`;
